apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mcp.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "mcp.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- if .Values.serviceAccount.create }}
---
# (keep separate resource only if create true)
{{- end }}
